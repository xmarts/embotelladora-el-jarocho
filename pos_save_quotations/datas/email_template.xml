<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!--Email template -->
        <record id="email_pos_quotation" model="mail.template">
            <field name="name">POS Quotation - Send by Email</field>
            <field name="email_from">${(object.user_id.email and '%s &lt;%s&gt;' % (object.user_id.name, object.user_id.email) or '')|safe}</field>
            <field name="subject">${object.company_id.name} 'Your Quotation'}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="model_id" ref="pos_save_quotations.model_pos_quotation"/>
            <field name="auto_delete" eval="True"/>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="body_html"><![CDATA[
            <p>Dear ${object.partner_id.name}


            % if object.partner_id.parent_id:
                (<i>${object.partner_id.parent_id.name}</i>)
            % endif
            ,</p>

            <p>
            Here is your order confirmation <strong>${object.name}</strong> amounting in <strong>${object.amount_total}</strong> <strong>${object.company_id.currency_id.name} from ${object.company_id.name}</strong>.
            </p>
            % if object.note:
                (
                <p>
                    With note ${object.name}
                </p>
                )
            % endif

            <p>You can reply to this email if you have any questions.</p>
            <p>Thank you,</p>

            <p style="color:#888888;">
            % if object.user_id and object.user_id.signature:
                ${object.user_id.signature | safe}
            % endif
            </p>
            ]]></field>
        </record>


    </data>
</odoo>
